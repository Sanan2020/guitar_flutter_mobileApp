import 'package:flutter/material.dart';

class chord_search extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          centerTitle: true,
          title: Text("chord search Page"),
        ),
        body: Center(
          child: Column(
            children: const <Widget>[Text("start microphone2......")],
          ),
        ),
      );
}
